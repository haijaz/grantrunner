# from bs4 import BeautifulSoup
from smtpd import SMTPServer

keyword = 'agriculture'
results = Scraper.scrape(keyword)
test = [s for s in results if s.keys()!=[]] 
x=0
for k in test:
	# for cell in k:
		# print cell + '=			' + k[cell].strip()
	# print ""
	print k['attachment']
	# if k.keys()==[]:
		# print x
	x +=1
	print x
print x